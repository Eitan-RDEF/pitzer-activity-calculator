# Legacy prototype

`pitzer_hplus_ui.py` is the original Tkinter desktop calculator that established the first
working H⁺ activity workflow. It is preserved as behavioral reference while the supported
calculation is moved behind tested domain and engine interfaces.

New features should be implemented in `src/pitzer_calculator/`, not in the legacy script.

